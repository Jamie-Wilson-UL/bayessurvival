% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{apply_halabi_singh_censoring}
\alias{apply_halabi_singh_censoring}
\title{Apply Halabi-Singh Censoring Method}
\usage{
apply_halabi_singh_censoring(
  survival_times,
  censoring_rate,
  censoring_distribution = "exponential",
  seed = NULL
)
}
\arguments{
\item{survival_times}{Vector of true survival times}

\item{censoring_rate}{Desired proportion of censored observations (0 to 1)}

\item{censoring_distribution}{Distribution for censoring times ("exponential" or "uniform")}

\item{seed}{Random seed for reproducibility}
}
\value{
Data frame with columns:
\itemize{
\item time: Observed time (min of survival time and censoring time)
\item status: Event indicator (1 = event, 0 = censored)
\item true_time: Original survival time (for validation)
\item censor_time: Censoring time used
}
}
\description{
Applies the Halabi-Singh censoring mechanism. This method generates realistic
censoring patterns with control over the censoring rate.
}
\details{
The Halabi-Singh method supports two censoring mechanisms:

\strong{Exponential Censoring (default):}
\itemize{
\item Censoring times: C ~ Exponential(β)
}

\strong{Uniform Censoring:}
\itemize{
\item Censoring times: C ~ Uniform(0, C)
\item May have issues at high censoring rates (>50\%)
}
}
\examples{
\dontrun{
# Generate Weibull survival times
true_times <- simulate_survival_data(100, "weibull", list(shape = 2, scale = 4))

# Apply 30\% exponential censoring 
censored_data <- apply_halabi_singh_censoring(true_times, censoring_rate = 0.3)

# Apply uniform censoring (for comparison)
censored_uniform <- apply_halabi_singh_censoring(
  true_times, censoring_rate = 0.3, censoring_distribution = "uniform"
)

# Check actual censoring rates
mean(censored_data$status == 0)
}
}
\references{
Halabi, S., & Singh, B. (2004)
}
