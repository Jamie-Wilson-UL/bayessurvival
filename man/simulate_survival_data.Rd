% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_survival_data}
\alias{simulate_survival_data}
\title{Generate Survival Data from Known Distribution}
\usage{
simulate_survival_data(n, distribution, params, seed = NULL)
}
\arguments{
\item{n}{Number of observations to generate}

\item{distribution}{Distribution family ("weibull", "exponential", "lognormal")}

\item{params}{Named list of distribution parameters}

\item{seed}{Random seed for reproducibility}
}
\value{
Vector of survival times
}
\description{
Generates survival times from a specified parametric distribution with known parameters.
This allows validation against ground truth.
}
\details{
Distribution parameters:
\itemize{
\item Weibull: list(shape = α, scale = λ)
\item Exponential: list(rate = λ)
\item Log-normal: list(meanlog = μ, sdlog = σ)
}
}
\examples{
\dontrun{
# Generate Weibull data 
times <- simulate_survival_data(100, "weibull", list(shape = 2, scale = 4))

# Generate exponential data
times <- simulate_survival_data(100, "exponential", list(rate = 0.5))
}
}
